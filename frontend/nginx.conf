server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing — SIEMPRE devuelve index.html si el archivo no existe
    # Sin =404 al final para que React Router maneje las rutas
    location / {
        try_files $uri $uri.html $uri/ /index.html;
    }

    # Next.js static assets con cache largo
    location /_next/static/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Health check
    location = /health {
        access_log off;
        return 200 "ok";
        add_header Content-Type text/plain;
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml;
}
